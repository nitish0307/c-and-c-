#include<stdio.h>
int main(void)
{
    int i, n, num;
    printf("enter the integer:");
    scanf("%d", &i);
    for(i>=1; i<500; i++)
    {
        if (i%n ! = 0);
        printf("prime number = %d", i);
        else("number is not prime");
    }
    getch();
    return 0;
}